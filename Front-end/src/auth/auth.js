export const isAuthenticated = () => true;

// const tokenUser - localStorage.getItem('token') 

// if(!token){
//     return <Redirect to=`/ideias/${sadsada}` />
// }
// else{
//     return <Redirect to="/ideias" />
// }